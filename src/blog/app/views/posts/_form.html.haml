= form_for(@post) do |f|
  - if @post.errors.any?
    %ul
      - @post.errors.full_messages.each do |msg|
        %li= msg
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_area :content, :rows => 5
  .actions
    = f.submit
